version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb-nba-container
    ports:
      - "27017:27017"
    volumes:
      - ./init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
    environment:
      MONGO_INITDB_DATABASE: nba
